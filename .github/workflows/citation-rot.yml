# Citation rot (#397) — the machine half of "does this prose still describe the
# code?". `scripts/check_citations.py` does the work; this only decides when it
# runs and where the text lands.
#
# One mode: on a PR, scoped to the files the diff touches. This catches rot at
# the moment it is created, by the person creating it, while the context is
# still in their head. It is ADVISORY and must never fail the build — a PR going
# red because an unrelated issue got stale is how a check earns the reflex to be
# skipped. Note the deliberate absence from gates.yml: the required checks stay
# the ones that must be green.
#
# There was a second mode: weekly, repo-wide, writing into one standing tracking
# issue (#407). Retired 2026-07-23 — the report was a log nobody acted on, and a
# permanently-open issue restating rot is not the same thing as fixing it. The
# check that earns its keep is this one, at the moment the rot is introduced.
#
# The repo-wide sweep still exists in `scripts/check_citations.py` and is worth
# running by hand before a release, or with `--memory-dir` to cover the memory
# files CI cannot see:
#
#     python scripts/check_citations.py --sha HEAD --out report.md
name: citation-rot

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  pull-request:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      issues: read           # the checker reads open issue bodies
      pull-requests: write   # ...and leaves one sticky comment
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0     # the diff against the base needs real history

      - uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      # The checker is stdlib-only on purpose: no install step, nothing to
      # cache, nothing that can rot on its own.
      - name: Files this PR touches
        run: |
          git fetch --no-tags origin "$GITHUB_BASE_REF"
          git diff --name-only "origin/$GITHUB_BASE_REF...HEAD" > changed.txt
          cat changed.txt

      - name: Check citations into those files
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python scripts/check_citations.py \
            --changed-files changed.txt \
            --no-mentions \
            --sha "$GITHUB_SHA" \
            --title "Citation rot in the files this PR touches" \
            --out report.md
          cat report.md

      # Sticky: one comment, edited on every push, so a long PR does not
      # accumulate a column of stale reports. Skipped entirely when there is
      # nothing to say and nothing said before.
      - name: Comment (advisory)
        continue-on-error: true   # a fork PR's token cannot write; not a failure
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          MARKER='<!-- citation-rot -->'
          printf '%s\n' "$MARKER" | cat - report.md > body.md
          # --paginate: a PR with more than 30 comments would otherwise miss
          # its own sticky comment and post a second one, every push.
          EXISTING=$(gh api --paginate "repos/$GITHUB_REPOSITORY/issues/$PR/comments" \
            --jq "map(select(.body | startswith(\"$MARKER\"))) | .[].id" | head -n1)
          if [ -z "$EXISTING" ] && grep -q 'No rot found.' report.md; then
            echo "Nothing to report and nothing said before."
            exit 0
          fi
          if [ -n "$EXISTING" ]; then
            gh api --method PATCH "repos/$GITHUB_REPOSITORY/issues/comments/$EXISTING" \
              -F body=@body.md
          else
            gh api --method POST "repos/$GITHUB_REPOSITORY/issues/$PR/comments" \
              -F body=@body.md
          fi
