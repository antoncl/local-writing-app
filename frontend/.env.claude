# Loaded by `vite --mode claude` (the frontend-claude launch config).
# Points the isolated Claude frontend (:5174) at the isolated backend (:8788),
# so Claude's verification never touches Anton's shared :8787/:5173 instance.
VITE_API_BASE=http://127.0.0.1:8788/api
