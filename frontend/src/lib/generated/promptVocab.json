{
  "_generated": "GENERATED from docs/prompts/reference.md by scripts/gen_prompt_vocab.py — do not edit by hand. Run `python scripts/gen_prompt_vocab.py` after changing the reference.",
  "symbols": [
    {
      "name": "date",
      "kind": "variable",
      "signature": "date",
      "summary": "Today's date; `date.year`, `date.month`, `date.day`, and `str(date)`."
    },
    {
      "name": "inputs",
      "kind": "variable",
      "signature": "inputs",
      "summary": "The prompt's declared inputs, by name. `inputs.<name>` — a `context_pick` resolves to a `list` of nodes (or use `entry(inputs.pick)` for the first); a `text`/`select` to a string; a number to a number."
    },
    {
      "name": "project",
      "kind": "variable",
      "signature": "project",
      "summary": "The project node. `project.<field>` reads an authored project field (e.g. `project.spelling`); `project.metadata` is the whole map; intrinsics (`title`, `root_path`) win a name collision."
    },
    {
      "name": "scene",
      "kind": "variable",
      "signature": "scene",
      "summary": "The prompt's scene node (`manuscript:scene`) or `None`. `scene.title`, `scene.body`, `scene.<field>` (e.g. `scene.pov`, `scene.summary`); entity-ref fields auto-resolve to nodes."
    },
    {
      "name": "selection",
      "kind": "variable",
      "signature": "selection",
      "summary": "`str` — the selected prose (revise/inline dispositions), else `\"\"`."
    },
    {
      "name": "text_after",
      "kind": "variable",
      "signature": "text_after",
      "summary": "`str` — body markdown around the cursor; `\"\"` when not dispatched from an editor."
    },
    {
      "name": "text_before",
      "kind": "variable",
      "signature": "text_before",
      "summary": "`str` — body markdown around the cursor; `\"\"` when not dispatched from an editor."
    },
    {
      "name": "character_turns",
      "kind": "helper",
      "signature": "character_turns(scene, character)",
      "summary": "Reconstructs the scene as **alternating chat turns** for the Roleplay sub-type (focus character → `assistant`, others → `user` prefixed `[Name]:`, narration → plain `user`). Emits its own role boundaries — use it **outside** any `{% role %}` block."
    },
    {
      "name": "entry",
      "kind": "helper",
      "signature": "entry(x, at=scene, position=n)",
      "summary": "A node resolved as of the prompt's scene (or the explicit `at=` scene; `at=None` forces book-start; `position=` is a within-scene cursor). `entry_type` inferred from `x`. Accepts an id, a node, or a `context_pick` value (first pick)."
    },
    {
      "name": "field_contract",
      "kind": "helper",
      "signature": "field_contract.stored",
      "summary": "The registered set, read back by the commit path as the shape to enforce. An empty contract commits nothing."
    },
    {
      "name": "field_value",
      "kind": "helper",
      "signature": "field_value(entity, field)",
      "summary": "The value of one field on `entity`, by id — the read-back companion to `fields()`. `field` is a bare id or a `fields()` descriptor, so a group loop reads each member's value: `{% for f in fields(e) if f.group == \"GMO\" %}{{ f.label }}: {{ field_value(e, f) }}{% endfor %}`. `title`/`body` return the node's intrinsics; an `entity_ref` value wraps to a node (so `field_value(e, \"patron\").title` works). `None` when unresolved."
    },
    {
      "name": "fields",
      "kind": "helper",
      "signature": "fields(x)",
      "summary": "The **full** field roster of a node or an `entry_type` FQN — a list of descriptors, each `{id, label, type, options, description, group, proposable}` (plus `items` for list fields; `tag_vocabulary` for a proposable tag-vocabulary field). `group` is the field's section label (an applied struct like `GMO`, or a manual header; `None` when ungrouped), so a template can group by it: `{% for f in fields(e) if f.group == \"GMO\" %}`. `proposable` is advisory; the template chooses what to show. `tag_vocabulary` is present only on a proposable tag-vocabulary `entity_ref_list` field (ADR-0082 §2) — the entry's existing tags, by usage, for a \"prefer tags that already exist\" prompt to point at."
    },
    {
      "name": "full_outline",
      "kind": "helper",
      "signature": "full_outline()",
      "summary": "The manuscript's outline: a nested list of nodes with `.title`, `.summary`, `.children`."
    },
    {
      "name": "full_text",
      "kind": "helper",
      "signature": "full_text()",
      "summary": "`str` — every scene's prose in reading order. Heavy — the whole-manuscript escape hatch. For one scene, use the prompt's target `scene` (`scene.body`); `use()` selects **lore**, not scene prose."
    },
    {
      "name": "is_a",
      "kind": "helper",
      "signature": "is_a(node, entry_type)",
      "summary": "`bool` — kind-of test against the type's `parent:` chain."
    },
    {
      "name": "last_words",
      "kind": "helper",
      "signature": "last_words(text, n)",
      "summary": "`str` — the trailing `n` words of a string (pure helper)."
    },
    {
      "name": "original",
      "kind": "helper",
      "signature": "original(x)",
      "summary": "The same node at **book-start**, ignoring every mutation."
    },
    {
      "name": "plot_context",
      "kind": "helper",
      "signature": "plot_context(as_of=node)",
      "summary": "`str` — the spoiler-gated plot-board recap up to `as_of` (a card/scene). Derived and emitted, like `story_so_far`."
    },
    {
      "name": "pov",
      "kind": "helper",
      "signature": "pov(scene)",
      "summary": "The scene's POV character (`lore:character`) or `None` — the scene's OWN `pov` field. For the value a scene *inherits*, use `resolved_narration`."
    },
    {
      "name": "resolved_narration",
      "kind": "helper",
      "signature": "resolved_narration(scene)",
      "summary": "The scene's **effective** narration folded down the manuscript structure (ADR-0079): `{mode, character}`. Unlike `pov(scene)`, this resolves inheritance — a scene inheriting its POV from its act or the book reports that value — and drops `character` for `third_omniscient` / `third_objective` modes. `character` is a `lore:character` node (so `resolved_narration(scene).character.title` works) or `None`; `mode` is the `pov_mode` value or `None`."
    },
    {
      "name": "roleplay_beats",
      "kind": "helper",
      "signature": "roleplay_beats(scene)",
      "summary": "`str` — the roleplay scene laid out **beat by beat** for a finalize/cleanup prompt: each beat's speaker, its observable text, and (decoded) its private interiority. POV-agnostic; the finalize prompt decides whose interiority survives via `pov(scene)`. A scene with no beat markers returns its body unchanged."
    },
    {
      "name": "story_so_far",
      "kind": "helper",
      "signature": "story_so_far(scene)",
      "summary": "`str` — an XML recap of prior scenes' summaries (scenes **1 → n-1**, reading order). A derived, per-scene-deterministic block: it is emitted (not selected) and caches in the stable prefix."
    },
    {
      "name": "type_name",
      "kind": "helper",
      "signature": "type_name(x)",
      "summary": "`str` — a type's human name (from an `entry_type` FQN)."
    },
    {
      "name": "use",
      "kind": "helper",
      "signature": "use(node, \"stable\"|\"volatile\")",
      "summary": "`\"\"` — adds the node to what the AI sees. It prints nothing where you write it: the app fetches the node and includes it for you, with no duplicates. Hand it a whole picker selection to add every pick at once. The optional `\"stable\"`/`\"volatile\"` hint nudges caching and can usually be left off."
    },
    {
      "name": "use_lore",
      "kind": "helper",
      "signature": "use_lore()",
      "summary": "`\"\"` — automatically includes the lore relevant to this scene: entries it links to, and entries named in its summary. Call it to turn that on; leave it out for no automatic lore."
    },
    {
      "name": "json",
      "kind": "filter",
      "signature": "value | json",
      "summary": "`str` — compact JSON, **insertion-order preserving**, no surprise HTML-escaping. The one JSON spelling (replaces `plain_json` / `tojson`)."
    },
    {
      "name": "do",
      "kind": "tag",
      "signature": "{% do … %}",
      "summary": "Evaluates an expression for its side effect and emits nothing (Jinja's `ext.do`) — the construct for the side-effecting helpers: `{% do use(node) %}` records a lore pick, `{% do field_contract.store(f) %}` registers a field."
    },
    {
      "name": "include",
      "kind": "tag",
      "signature": "{% include \"Snippet title\" %}",
      "summary": "Inlines a `prompt:snippet` node by title (e.g. `Project settings`)."
    },
    {
      "name": "role",
      "kind": "tag",
      "signature": "{% role \"system\"|\"user\"|\"assistant\" %}…{% endrole %}",
      "summary": "Marks the wrapped content's message role. An override: un-roled prose is homed to the base type's default role (usually `system`), so a prose-only prompt just works."
    }
  ]
}
